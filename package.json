{
  "name": "lambda_builder",
  "version": "1.0.0",
  "dependencies": {
    "uuid": "^8.1.0"
  },
  "devDependencies": {
    "jest": "^26.6.3"
  },
  "scripts": {
    "test": "jest --silent --collectCoverage --colors"
  },
  "jest": {
    "setupFiles": [
      "<rootDir>/jest/setEnvVars.js"
    ],
    "coverageDirectory": "<rootDir>/jest/coverage"
  }
}
